export const homeVisualConfig = {
  canvas: {
    // Homepage canvas backing color. Glass blur/refraction is only visible when there is contrast behind it.
    backgroundColor: "#25272b",
  },
  flowText: {
    // Larger values make the color gradient travel faster along the handwritten path.
    colorSpeed: 12,
  },
};
